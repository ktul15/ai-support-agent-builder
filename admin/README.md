# admin (Next.js builder dashboard)

The SMB-facing builder: signup/login, document upload with live ingestion
progress, the retrieval playground, publish flow, and API-key management.

**Not yet initialized.** Scaffolded as a placeholder in issue #2; the Next.js
app is created in Phase 3 (issue #29 onward). It lints/builds with its own
toolchain and is intentionally excluded from the root npm workspaces and the
shared ESLint config.
